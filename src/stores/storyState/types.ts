
// Import from our centralized type definitions
import { 
  CustomStory,
  StoryState,
  StoryActions,
  StoryStore,
  TabType
} from '@/types';

// Re-export all the types for backward compatibility
export type {
  CustomStory,
  StoryState,
  StoryActions,
  StoryStore,
  TabType
};
