
// Import the shared types from our definition file
import { 
  CustomStory,
  StoryState,
  StoryActions,
  StoryStore,
  TabType
} from '@/types/story-types.definitions';

// Re-export all the types for backward compatibility
export {
  CustomStory,
  StoryState,
  StoryActions,
  StoryStore,
  TabType
};
